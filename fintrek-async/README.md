# Финтрек Backend - Этап 3: Open API & Data Aggregation

Реализация интеграции с банковскими Open API и механизма агрегации данных для проекта "Финтрек".

## Что реализовано в этапе 3

### 1. Модели данных

- **Account** - банковские счета пользователей
- **Transaction** - финансовые транзакции
- **Category** - категории доходов и расходов
- **BankConnection** - подключения к банкам через Open API

### 2. API эндпоинты

#### Управление счетами (`/api/v1/accounts`)
- `GET /` - получить список всех счетов
- `GET /{account_id}` - получить информацию о счете
- `POST /` - создать новый счет вручную
- `PATCH /{account_id}` - обновить счет
- `DELETE /{account_id}` - удалить счет

#### Управление транзакциями (`/api/v1/transactions`)
- `GET /` - получить список транзакций с фильтрацией и пагинацией
- `GET /{transaction_id}` - получить информацию о транзакции
- `POST /` - создать новую транзакцию
- `PATCH /{transaction_id}` - обновить транзакцию
- `DELETE /{transaction_id}` - удалить транзакцию

#### Управление категориями (`/api/v1/categories`)
- `GET /` - получить список категорий
- `GET /{category_id}` - получить информацию о категории
- `POST /` - создать новую категорию
- `PATCH /{category_id}` - обновить категорию
- `DELETE /{category_id}` - удалить категорию

#### Подключения к банкам (`/api/v1/bank-connections`)
- `GET /` - получить список подключений
- `GET /{connection_id}` - получить информацию о подключении
- `POST /` - создать новое подключение
- `POST /sync` - синхронизировать данные с банком
- `DELETE /{connection_id}` - отключить банк

### 3. Сервисы интеграции

#### OpenBankingService
Сервис для работы с банковскими Open API:
- OAuth 2.0 / OpenID Connect аутентификация
- Получение списка счетов
- Получение транзакций
- Обновление токенов доступа

#### SyncService
Сервис синхронизации данных:
- Автоматическая синхронизация счетов
- Синхронизация транзакций
- Обработка ошибок и повторные попытки
- Шифрование токенов доступа

### 4. Безопасность

- Шифрование токенов доступа к банковским API (Fernet)
- Валидация данных через Pydantic
- Проверка прав доступа к ресурсам
- Обработка ошибок и логирование

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Добавьте в `.env` новые переменные для работы с Open Banking API:

```env
# Open Banking API
OPEN_BANKING_API_URL=https://api.example-bank.ru
OPEN_BANKING_CLIENT_ID=your_client_id
OPEN_BANKING_CLIENT_SECRET=your_client_secret

# Ключ шифрования (должен быть 32 символа)
ENCRYPTION_KEY=your_32_character_encryption_key
```

### 3. Применение миграций

```bash
alembic upgrade head
```

### 4. Запуск приложения

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## Документация API

После запуска приложения документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## Примеры использования

### 1. Создание счета

```bash
curl -X POST "http://localhost:8000/api/v1/accounts/" \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "account_name": "Основной счет",
    "account_type": "checking",
    "currency": "RUB",
    "balance": 10000.00
  }'
```

### 2. Получение списка транзакций

```bash
curl -X GET "http://localhost:8000/api/v1/transactions/?page=1&page_size=20" \
  -H "Authorization: Bearer <access_token>"
```

### 3. Создание категории

```bash
curl -X POST "http://localhost:8000/api/v1/categories/" \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Продукты",
    "category_type": "expense",
    "icon": "🛒",
    "color": "#FF5733"
  }'
```

### 4. Синхронизация с банком

```bash
curl -X POST "http://localhost:8000/api/v1/bank-connections/sync" \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "connection_id": "uuid-of-connection"
  }'
```

## Интеграция с Open Banking API

### OAuth 2.0 Flow

1. Пользователь инициирует подключение к банку
2. Система перенаправляет на страницу авторизации банка
3. Пользователь авторизуется в банке
4. Банк возвращает authorization code
5. Система обменивает code на access и refresh токены
6. Токены шифруются и сохраняются в БД

### ГОСТ-шлюз

Для работы с ГОСТ-шлюзом (требование хакатона VTB):

1. Настройте подключение к ГОСТ-шлюзу в `open_banking_service.py`
2. Используйте сертификаты для аутентификации
3. Все запросы к банковским API должны проходить через шлюз

## Следующие шаги (Этап 4)

После завершения этапа 3, переходите к этапу 4:
- Разработка AI-советника
- Категоризация транзакций с помощью ML
- Анализ паттернов расходов
- Система рекомендаций
- Прогностическая модель

## Безопасность

⚠️ **Важные замечания по безопасности:**

1. Токены доступа к банковским API шифруются перед сохранением
2. Используйте HTTPS для всех запросов к банковским API
3. Регулярно обновляйте токены доступа
4. Храните ключи шифрования в безопасном месте
5. Логируйте все операции с финансовыми данными
6. Используйте ГОСТ-шлюз для соответствия требованиям ЦБ РФ

## Лицензия

Проект разработан для хакатона VTB Multibank.
